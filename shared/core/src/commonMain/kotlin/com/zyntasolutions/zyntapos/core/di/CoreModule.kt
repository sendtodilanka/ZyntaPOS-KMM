package com.zyntasolutions.zyntapos.core.di

/**
 * ZentaPOS — :shared:core Koin DI Module
 *
 * Placeholder for Sprint 2 (Step 1.2.12) where real bindings are added:
 * - ZentaLogger (Kermit wrapper)
 * - Coroutine dispatchers: IO / Main / Default
 *
 * Registered in the root Koin graph via `startKoin { modules(coreModule) }`.
 */
internal object CoreModule
